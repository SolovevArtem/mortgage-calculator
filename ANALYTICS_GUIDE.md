# 📊 Руководство по настройке аналитики

Полная инструкция по настройке системы аналитики для Telegram Mini App.

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Что отслеживается](#что-отслеживается)
3. [Варианты развёртывания](#варианты-развёртывания)
4. [Быстрый старт](#быстрый-старт)
5. [Production развёртывание](#production-развёртывание)
6. [Просмотр аналитики](#просмотр-аналитики)
7. [API документация](#api-документация)

---

## 🎯 Обзор системы

Система аналитики состоит из трёх компонентов:

```
┌─────────────────┐
│  Telegram Mini  │ ← Пользователь взаимодействует
│      App        │   
└────────┬────────┘
         │ События отправляются
         ▼
┌─────────────────┐
│  Analytics API  │ ← Принимает и сохраняет события
│   (Node.js)     │
└────────┬────────┘
         │ Данные читаются
         ▼
┌─────────────────┐
│   Dashboard     │ ← Визуализация статистики
│     (HTML)      │
└─────────────────┘
```

---

## 📊 Что отслеживается

### Автоматические события:
- ✅ **app_opened** - Открытие приложения
- ✅ **app_closed** - Закрытие приложения
- ✅ **slider_changed** - Изменение слайдеров (price, down_payment, term, rate)
- ✅ **calculation_performed** - Выполнен расчёт ипотеки
- ✅ **share_clicked** - Нажатие кнопки "Поделиться"
- ✅ **time_spent** - Время, проведённое в приложении

### Собираемые данные:
```javascript
{
  session_id: "session_1234567890_abc123",
  user_id: 123456789,
  user_info: {
    id: 123456789,
    first_name: "Иван",
    username: "ivan_user",
    language_code: "ru"
  },
  events: [
    {
      event_name: "calculation_performed",
      timestamp: "2024-02-08T15:30:00.000Z",
      properties: {
        price: 5000000,
        down_payment: 1000000,
        term_years: 15,
        rate: 12,
        monthly_payment: 48049,
        calculation_number: 5
      }
    }
  ]
}
```

---

## 🚀 Варианты развёртывания

### Вариант 1: Локальное тестирование (самый простой)
**Для:** MVP и первых тестов  
**Стоимость:** Бесплатно  
**Сложность:** ⭐ Очень просто

### Вариант 2: Бесплатный хостинг (рекомендуется для MVP)
**Для:** Публичного тестирования  
**Платформы:** Render.com, Railway.app, Fly.io  
**Стоимость:** Бесплатно (с ограничениями)  
**Сложность:** ⭐⭐ Просто

### Вариант 3: Google Sheets (без сервера!)
**Для:** Минимальной аналитики без сервера  
**Стоимость:** Бесплатно  
**Сложность:** ⭐ Очень просто

### Вариант 4: Production сервер
**Для:** Полноценного продукта  
**Платформы:** DigitalOcean, AWS, Heroku  
**Стоимость:** От $5/месяц  
**Сложность:** ⭐⭐⭐ Средне

---

## ⚡ Быстрый старт

### Локальное тестирование (5 минут)

**Шаг 1: Установка зависимостей**
```bash
npm install
```

**Шаг 2: Запуск сервера**
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

**Шаг 3: Тестирование**
Откройте в браузере:
- Dashboard: `http://localhost:3000/api/dashboard`
- Health: `http://localhost:3000/health`

**Шаг 4: Настройка Mini App**

В файле `mortgage-calculator-analytics.html` найдите:
```javascript
this.apiEndpoint = 'YOUR_ANALYTICS_ENDPOINT';
```

Замените на:
```javascript
this.apiEndpoint = 'http://localhost:3000/api/analytics';
```

**Шаг 5: Откройте приложение**

Откройте `mortgage-calculator-analytics.html` в браузере и попользуйтесь калькулятором. События будут отправляться на локальный сервер.

**Шаг 6: Просмотр дашборда**

Откройте `analytics-dashboard.html` в браузере. В настройках измените URL:
```javascript
const API_BASE = 'http://localhost:3000/api';
```

---

## 🌐 Production развёртывание

### Вариант А: Render.com (рекомендуется)

**1. Подготовка репозитория**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_GITHUB_REPO
git push -u origin main
```

**2. Развёртывание на Render**
1. Зайдите на [render.com](https://render.com)
2. Создайте аккаунт
3. New → Web Service
4. Подключите ваш GitHub репозиторий
5. Настройки:
   - Name: `mortgage-analytics`
   - Environment: `Node`
   - Build Command: `npm install`
   - Start Command: `npm start`
6. Нажмите "Create Web Service"

**3. Получите URL**
После развёртывания получите URL типа:
```
https://mortgage-analytics.onrender.com
```

**4. Обновите Mini App**
В `mortgage-calculator-analytics.html`:
```javascript
this.apiEndpoint = 'https://mortgage-analytics.onrender.com/api/analytics';
```

**5. Обновите Dashboard**
В `analytics-dashboard.html`:
```javascript
const API_BASE = 'https://mortgage-analytics.onrender.com/api';
```

---

### Вариант Б: Railway.app

**1. Установка Railway CLI**
```bash
npm i -g @railway/cli
```

**2. Логин и развёртывание**
```bash
railway login
railway init
railway up
```

**3. Получение домена**
```bash
railway domain
```

Используйте полученный URL в настройках приложения.

---

### Вариант В: Google Sheets (без сервера)

Если не хотите поднимать сервер, можно отправлять данные напрямую в Google Sheets!

**1. Создайте Google Apps Script**

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Tools → Script Editor
4. Вставьте код:

```javascript
function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);
  
  data.events.forEach(event => {
    sheet.appendRow([
      new Date(event.timestamp),
      event.event_name,
      data.user_id,
      JSON.stringify(event.properties)
    ]);
  });
  
  return ContentService.createTextOutput(JSON.stringify({
    success: true
  })).setMimeType(ContentService.MimeType.JSON);
}
```

5. Deploy → New deployment → Web app
6. Скопируйте URL

**2. Обновите Mini App**
```javascript
this.apiEndpoint = 'YOUR_GOOGLE_SCRIPT_URL';
```

---

## 📈 Просмотр аналитики

### Метод 1: Web Dashboard

Откройте `analytics-dashboard.html` в браузере после настройки API_BASE.

### Метод 2: API напрямую

**Дашборд (все метрики):**
```bash
curl https://your-server.com/api/dashboard
```

**Статистика:**
```bash
curl https://your-server.com/api/stats
```

**Последние события:**
```bash
curl https://your-server.com/api/events/recent?limit=50
```

**События пользователя:**
```bash
curl https://your-server.com/api/user/123456789
```

### Метод 3: Файлы напрямую

Все данные сохраняются в директории `analytics-data/`:

- `events.jsonl` - все события (JSONL формат)
- `stats.json` - агрегированная статистика

---

## 🔌 API документация

### POST /api/analytics
Принимает события от Mini App.

**Request:**
```json
{
  "session_id": "session_123",
  "user_id": 123456789,
  "user_info": {
    "id": 123456789,
    "first_name": "Иван"
  },
  "events": [
    {
      "event_name": "calculation_performed",
      "timestamp": "2024-02-08T15:30:00.000Z",
      "properties": {
        "price": 5000000,
        "rate": 12
      }
    }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "events_received": 1
}
```

---

### GET /api/dashboard
Получить сводную аналитику.

**Response:**
```json
{
  "total_sessions": 150,
  "total_events": 1250,
  "unique_users": 87,
  "events_by_type": {
    "app_opened": 150,
    "calculation_performed": 650,
    "slider_changed": 400,
    "share_clicked": 50
  },
  "calculations": {
    "total": 650,
    "avg_price": 6500000,
    "avg_rate": "11.50"
  }
}
```

---

### GET /api/stats
Получить общую статистику.

**Response:**
```json
{
  "total_sessions": 150,
  "total_events": 1250,
  "total_calculations": 650,
  "total_shares": 50,
  "unique_users": 87
}
```

---

### GET /api/events/recent?limit=100
Получить последние события.

**Response:**
```json
{
  "count": 20,
  "events": [...]
}
```

---

### GET /api/user/:userId
Получить данные конкретного пользователя.

**Response:**
```json
{
  "user_id": 123456789,
  "sessions_count": 5,
  "total_events": 47,
  "sessions": [...]
}
```

---

## 🔧 Расширенная настройка

### Добавление нового типа событий

В `mortgage-calculator-analytics.html` добавьте метод:

```javascript
analytics.trackCustomEvent('button_clicked', {
  button_name: 'calculate',
  position: 'header'
});
```

### Изменение частоты отправки

```javascript
// В конструкторе Analytics
setInterval(() => this.flush(), 60000); // Каждую минуту
```

### Добавление webhook уведомлений

В `analytics-server.js`:

```javascript
async function sendWebhook(data) {
  await fetch('YOUR_WEBHOOK_URL', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
}
```

---

## 🐛 Отладка

### Проверка отправки событий

В браузере откройте DevTools → Console. Должны быть логи:
```
📊 Analytics Event: app_opened {...}
📤 Sending analytics: {...}
```

### Проверка получения на сервере

В терминале сервера должно быть:
```
📊 Received 5 events from session session_123
```

### Проверка сохранения данных

```bash
cat analytics-data/events.jsonl | tail -n 5
```

---

## 📊 Расшифровка метрик

### Ключевые показатели для MVP:

1. **Conversion Rate (CR)**
   - Формула: (Пользователи с расчётом / Всего открытий) × 100%
   - Цель: > 70%

2. **Engagement**
   - Формула: Среднее количество расчётов на сессию
   - Цель: > 3

3. **Share Rate**
   - Формула: (Нажатия "Поделиться" / Всего расчётов) × 100%
   - Цель: > 5%

4. **Session Duration**
   - Среднее время в приложении
   - Цель: > 2 минуты

---

## 🎯 Что анализировать в MVP

### День 1-7:
- ✅ Сколько человек открыли приложение?
- ✅ Сколько сделали хотя бы один расчёт?
- ✅ Какие диапазоны цен и ставок популярны?

### День 8-30:
- ✅ Возвращаются ли пользователи?
- ✅ Какая средняя длительность сессии?
- ✅ Где отваливаются пользователи?

### Месяц 2+:
- ✅ Cohort анализ
- ✅ Retention rate
- ✅ Влияние изменений на метрики

---

## 🔐 Безопасность

### Production checklist:

- [ ] Добавьте HTTPS (обязательно для Telegram)
- [ ] Ограничьте CORS только для вашего домена
- [ ] Добавьте rate limiting
- [ ] Валидируйте входящие данные
- [ ] Логируйте ошибки
- [ ] Настройте мониторинг (UptimeRobot, Pingdom)

Пример rate limiting:
```bash
npm install express-rate-limit
```

```javascript
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // макс 100 запросов
});

app.use('/api/', limiter);
```

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи сервера
2. Откройте DevTools → Console в браузере
3. Убедитесь, что CORS настроен правильно
4. Проверьте, что URL endpoints указаны верно

---

## 📚 Дополнительные ресурсы

- [Telegram Mini Apps документация](https://core.telegram.org/bots/webapps)
- [Express.js документация](https://expressjs.com/)
- [Render.com guides](https://render.com/docs)

---

Готово! Теперь у вас есть полноценная система аналитики для вашего MVP 🚀
